<div class="buttons-wrapper">
  <a href="/" class="logo">pastebin</a>
  <div class="buttons">
    <a href="/new" class="btn">New</a>
    <% if(locals.canSave) { %>
    <button class="btn" type="submit">Save</button>
    <%}%> <% if(locals.id != null) { %>
    <a href="/<%= locals.id %>/new/duplicate" class="btn">Duplicate</a>
    <%}%>
  </div>
</div>
